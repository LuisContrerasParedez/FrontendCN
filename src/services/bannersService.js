// El esquema público actual no expone banners de campaña. El hero de inicio usa
// la temática mensual y su estilo estático, por lo que se devuelve una lista vacía.
export async function obtenerBanners() {
  return [];
}
