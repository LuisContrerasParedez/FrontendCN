// El esquema público actual no expone páginas de CMS. Cada página del sitio usa
// sus títulos/textos estáticos por defecto, por lo que se devuelve una lista vacía.
export async function obtenerPaginas() {
  return [];
}
