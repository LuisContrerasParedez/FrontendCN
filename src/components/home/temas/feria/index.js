/*
 * Punto de entrada de la temática "Feria".
 * Todo lo suyo (hero, escena SVG, escenografía, estilos) vive en esta carpeta;
 * fuera solo se conoce este export.
 */
export { default } from './HeroFeriaAgosto';
